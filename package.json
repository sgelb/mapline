{
  "name": "mapline",
  "version": "0.23.0",
  "description": "Create maps along a track in print quality.",
  "main": "dist/index.html",
  "keywords": [],
  "homepage": "https://github.com/sgelb/mapline",
  "author": "sgelb",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "serve": "parcel serve index.html",
    "build": "parcel build --no-content-hash --no-cache --no-source-maps --detailed-report info index.html"
  },
  "browserslist": ["defaults", "not Explorer < 999", "not Android < 999"],
  "dependencies": {
    "@derhuerst/query-overpass": "^2.0.0",
    "@mapbox/geojson-normalize": "^0.0.1",
    "@mapbox/mapbox-gl-language": "^0.10.0",
    "@mapbox/togeojson": "^0.16.0",
    "cheap-ruler": "^3.0.1",
    "jspdf": "^2.2.0",
    "mapbox-gl": "^2.0.0"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1"
  }
}
